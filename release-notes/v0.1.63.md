# INVminer v0.1.63

## GPU command

```bash
./invminer --coin noid \
  -o stratum+ssl://eu.innovlab.cc:19601 \
  -u YOUR_NOID_ADDRESS.RIG_NAME
```

Use only an official NOID TLS endpoint: `eu.innovlab.cc:19601` (Europe) or
`hk.innovlab.cc:19601` (Hong Kong). This example defaults to Europe; replace
only the hostname to use Hong Kong. `-p/--pass` is optional; omission uses the
compatible default value `x`.

## CPU-only command

```bash
./invminer --coin noid --cpu-only \
  -o stratum+ssl://eu.innovlab.cc:19601 \
  -u YOUR_NOID_ADDRESS.RIG_NAME
```

> **Binary-only risk:** this release may not work with every GPU, CPU, driver,
> OS, or future pool state. Source is not provided here, so users cannot
> independently rebuild, audit, or patch it and must decide whether to trust
> the executable and published checksums. The software is provided without
> warranty.

## Ampere qualification update

This release adds an automatically selected execution profile for the exact
RTX 3080 10GB model on current drivers. Other GPU-model and architecture
selections remain unchanged. The CUDA 12/HiveOS flavor retains its reviewed
primary SM86 image, older-driver native image, and embedded CUDA 12.2 native
SM86 compatibility fallback; selection is automatic and requires no module or
geometry setting.

The password option remains optional for CLI, JSON, and HiveOS use. An omitted
CLI password or empty HiveOS Pass uses `x`. User-pool login also carries bounded
GPU-summary and miner-version metadata; pools that ignore the optional metadata
remain compatible.

## Validated RTX 3080 sample profile

The following profile was validated on the qualified RTX 3080 10GB sample with
NVIDIA Linux driver 580.178.04. It uses no core lock; the memory lock and core
offset are applied by INVminer:

```bash
./invminer --coin noid --devices 0 \
  --power-limit 320 \
  --lock-mem 810 \
  --core-clock-offset 260 \
  --nvidia-display :0 \
  --nvidia-xauthority /var/run/lightdm/root/:0 \
  -o stratum+ssl://eu.innovlab.cc:19601 \
  -u YOUR_NOID_ADDRESS.RIG_NAME
```

On the qualified RTX 3080 10GB sample, a warm 60-second run at the 320 W power cap produced 64.386 MH/s.

The offset is sample-specific and is not guaranteed on every RTX 3080. If an
optional setting cannot be applied, INVminer warns, restores the affected
hardware state, and continues at verified defaults when safe.

## Validated RTX 4090 default profile

The public `invminer-0.1.63.tar.gz` HiveOS package was validated on an RTX 4090
with NVIDIA Linux driver 595.84. The test used the GPU's default 450 W power
limit and default clocks; no device, power, lock, or offset option was passed:

```bash
./invminer --coin noid --benchmark 60
```

On the qualified RTX 4090 sample, the public HiveOS package produced 148.972 MH/s in a 60-second offline run at default clocks and the default 450 W power limit.

The exact self-test passed, the process exited successfully, NVIDIA Xid count
remained unchanged, and the GPU returned to its idle default state after the
run. Boost clocks and results can vary with card, cooling, firmware, driver,
ambient temperature, and host configuration.

## Validated RTX 4090 system-tuned profile

A second 60-second run used the public CUDA 13 asset on the same RTX 4090 and
driver. The operating system applied the settings before INVminer started;
INVminer itself received no power, clock, memory-lock, offset, or device option:

```bash
sudo nvidia-smi -i 0 -pl 450
sudo nvidia-smi -i 0 -lmc 810,810
sudo env DISPLAY=:0 XAUTHORITY=/path/to/Xauthority \
  nvidia-settings -a '[gpu:0]/GPUGraphicsClockOffsetAllPerformanceLevels=250'

./invminer --coin noid \
  -o stratum+ssl://eu.innovlab.cc:19601 \
  -u YOUR_NOID_ADDRESS.RIG_NAME
```

With the operating-system profile set to a 450 W power limit, 810 MHz locked memory, and +250 MHz core offset, the qualified RTX 4090 sample produced 161.447 MH/s in a 60-second offline run.

Average board power was 438.901 W and the maximum observed temperature was
82 C. The benchmark invocation was
`./invminer --coin noid --benchmark 60`; the miner reported `tuning=default`,
confirming that the settings came from the operating system.

This profile is sample-specific. Start with a lower core offset, monitor
temperature and accepted/rejected work, and increase only after stability is
confirmed. The X display and authority path depend on the host. Because these
settings are applied outside INVminer, INVminer does not own or automatically
restore them. Restore defaults explicitly when finished:

```bash
sudo nvidia-smi -i 0 -rgc
sudo nvidia-smi -i 0 -rmc
sudo env DISPLAY=:0 XAUTHORITY=/path/to/Xauthority \
  nvidia-settings -a '[gpu:0]/GPUGraphicsClockOffsetAllPerformanceLevels=0'
```

## Release assets

| Asset | Purpose | Embedded lanes | Archive SHA-256 | Binary SHA-256 |
|---|---|---|---|---|
| `invminer-v0.1.63-linux-x86_64-cuda12.tar.gz` | Broad/535-era host compatibility | `sm_75`, `sm_86`, `sm_89`, `sm_120`, fallback | `d1ecd9657b6ec68f759b2bdf48856ead192304e521df50be6af281006a6b0654` | `8535cc35c6163c0747e83149655b928a11fc62592b8178a91fa9f43764048fe9` |
| `invminer-v0.1.63-linux-x86_64-cuda13.tar.gz` | Driver API 13.0 / Linux driver 580+ | `sm_86`, `sm_89`, `sm_120` | `3e94445f2fd6ccf893d2f03c90c8acf4151f2f3f3c0948ad80c0d25c337bd9c1` | `ece2ccb69ba3f2c688f7da9bd249c8cdfcf3f930cabd5da7fc69b54392748900` |
| `invminer-0.1.63.tar.gz` | Canonical HiveOS package; broad CUDA 12 host flavor | same CUDA 12 lanes | `f2fb7073a7637e98e3b50c10c6617d653ccf77a66f0600a1f3df84f50d5eb1fa` | `8535cc35c6163c0747e83149655b928a11fc62592b8178a91fa9f43764048fe9` |

`SHA256SUMS.txt` SHA-256:
`e84cacffced48ff0b8a725a316162fd02137397c06b8acc258d600ee0134809a`.

Both flavors were reproducibly linked in isolated Ubuntu 20.04 / Rust 1.96
builders. The maximum required GNU C Library symbol is `GLIBC_2.30`; CUDA and
NVML are loaded through the host driver.

## Compatibility qualification

| GPU family | SM | CUDA 12 | CUDA 13 | Tested in this release | Functional result |
|---|---:|---|---|---|---|
| RTX 2080 Ti / Turing CMP | `sm_75` | Supported | Unsupported | No | Existing reviewed CUDA 12 path retained |
| RTX 3070 Ti / RTX 3090 | `sm_86` | Supported | Supported | No | Existing reviewed lanes and automatic CUDA 12 fallback retained |
| RTX 3080 10GB | `sm_86` | Supported | Supported | Yes — exact final CUDA 12 binary | Minimal user command at default hardware settings reached official TLS `ONLINE`, accepted work with no rejected work, and passed exact self-test with Pass omitted; CUDA 13 was not physically rerun |
| RTX 4070 | `sm_89` | Supported | Supported | Yes — exact canonical HiveOS/CUDA 12 package | Official TLS reached `ONLINE`, accepted work with no stale or rejected work, and passed HiveOS statistics with empty Pass |
| RTX 4070 SUPER / RTX 4070 Ti | `sm_89` | Supported | Supported | No | Existing reviewed Ada path retained |
| RTX 4090 | `sm_89` | Supported | Supported | Yes — exact canonical HiveOS/CUDA 12 package | Exact self-test and 60-second offline benchmark completed at default clocks and the default 450 W power limit; Xid count remained unchanged |
| RTX 5090 / RTX 50 | `sm_120` | Supported | Supported | No | Existing reviewed Blackwell path retained |

The CUDA 12 asset requires NVIDIA Linux driver 535 or newer. The CUDA 13 asset
requires Driver API 13.0 / Linux driver 580 or newer. CUDA 13 physical-GPU,
multi-GPU, disconnect/failover injection, long-duration, and the GPU models
marked “No” above were not rerun for this release.

## Native NVIDIA controls

The existing `--power-limit`, `--lock-core`, `--locked-core-clock`,
`--lock-mem`, and `--core-clock-offset` controls are retained. Unsupported
values, missing permissions, or unavailable optional tools produce a warning
and mining continues at the verified original/default state; an unsafe partial
rollback stops mining.

## HiveOS

- Miner name: `invminer`
- Installation URL: `https://github.com/getrigeos/INVminer-Release/releases/download/v0.1.63/invminer-0.1.63.tar.gz`
- Hash algorithm: `noid`
- Pool URL: `stratum+ssl://eu.innovlab.cc:19601`
- Wallet and worker template: `%WAL%.%WORKER_NAME%`
- Pass: leave empty (or enter `x`; both use the compatible default)
- Extra config arguments: leave empty unless ordinary runtime options are needed

The HiveOS archive must remain `<miner-name>-<version>.tar.gz`; do not add the
tag `v`, platform, architecture, HiveOS, or CUDA labels.

### Manual upgrade for older HiveOS installations

Only some older HiveOS installations need this manual replacement. Use it only when HiveOS does not update the installed custom miner after the Installation URL is changed to this release.

```bash
miner stop && cd /tmp && rm -rf invminer && rm -f invminer-0.1.63.tar.gz && wget -O invminer-0.1.63.tar.gz https://github.com/getrigeos/INVminer-Release/releases/download/v0.1.63/invminer-0.1.63.tar.gz && tar -xzf invminer-0.1.63.tar.gz && mkdir -p /hive/miners/custom/invminer && rm -f /hive/miners/custom/invminer/invminer && cp -af invminer/. /hive/miners/custom/invminer/ && chmod +x /hive/miners/custom/invminer/invminer /hive/miners/custom/invminer/h-*.sh && /hive/miners/custom/invminer/invminer --version && miner start
```

### 舊版 HiveOS 手動升級

僅部分舊版 HiveOS 需要此手動覆蓋；只有在更新 Installation URL 後仍未替換已安裝的 Custom Miner 時才執行。

## Developer fee and startup boundary

NOID uses a 5% developer fee measured from effective mining time. Waiting,
connection preparation failures, and unavailable fee work are not charged.
Extracting or running the package does not install or enable a boot service,
cron job, scheduled task, login/startup item, or container restart policy.

---

# INVminer v0.1.63（繁體中文）

本版為目前驅動上的 RTX 3080 10GB 精確型號加入自動選擇的執行設定；其他 GPU 型號與架構
選擇維持不變。CUDA 12／HiveOS 套件保留既有 SM86 主映像、舊驅動原生映像與內嵌
CUDA 12.2 原生 SM86 相容 fallback，全程自動選擇，不需手動指定模組或幾何參數。

`-p/--pass` 並非必填；CLI 省略或 HiveOS Pass 留空時會使用相容預設值 `x`。使用者礦池
登入會攜帶有界的 GPU 摘要與礦工版本中繼資料，不處理選用中繼資料的礦池仍維持相容。

本版亦公開 RTX 3080 10GB 合格樣卡設定：功率牆 320 W、顯存鎖定 810 MHz、核心 offset
`+260 MHz`，不使用核心鎖頻；核心 offset 所需的 display 與 Xauthority 參數如上方完整命令。
合格的 RTX 3080 10GB 樣卡在 320 W 功率牆下進行熱卡 60 秒測試，實測為 64.386 MH/s。
此 offset 僅代表該張樣卡，不保證每張 RTX 3080 都能使用；選用設定失敗時，礦工會在安全可恢復
的情況下還原硬體並以已驗證的預設狀態繼續。

本版亦使用公開 `invminer-0.1.63.tar.gz` HiveOS 套件，在 NVIDIA Linux Driver 595.84 的
RTX 4090 上執行 `./invminer --coin noid --benchmark 60`。測試未傳入裝置、功率、鎖頻或
offset 參數，使用顯卡預設 450 W 功率牆與預設頻率。
合格的 RTX 4090 樣卡使用公開 HiveOS 套件、預設 450 W 功率牆與預設頻率進行離線 60 秒測試，實測為 148.972 MH/s。
精確自檢通過、程序正常退出、NVIDIA Xid 數量未變，測試後 GPU 已回到空閒預設狀態；實際
Boost 頻率與結果仍會受到卡片、散熱、韌體、驅動、環境溫度及主機設定影響。

第二個環境使用同一張 RTX 4090、同一 Driver 595.84 與公開 CUDA 13 資產。啟動 INVminer 前，
由作業系統依照上方命令設定 450 W 功率牆、鎖定顯存 810 MHz 與核心 offset `+250 MHz`；
INVminer 本身未傳入功率、鎖頻、顯存鎖、offset 或裝置參數，正常挖礦命令亦如上所示。
作業系統設定 450 W 功率牆、顯存鎖定 810 MHz 與核心 offset +250 MHz 後，合格的 RTX 4090 樣卡進行離線 60 秒測試，實測為 161.447 MH/s。
平均板卡功耗為 `438.901 W`、最高溫度 `82 C`。實測命令為
`./invminer --coin noid --benchmark 60`，礦工回報 `tuning=default`，證明調優來自作業系統。

此設定僅代表該張樣卡。建議由更低的核心 offset 起步，持續監看溫度與 accepted/rejected 工作，
確認穩定後再逐步提高；X display 與 Xauthority 路徑依主機而異。由於設定在 INVminer 外部套用，
INVminer 不會自動還原；停止後必須執行上方 `-rgc`、`-rmc` 及 offset `0` 的還原命令。

RTX 3080 10GB 已使用最終 CUDA 12 二進位，以預設硬體設定及省略 Pass 的最簡使用者命令
連接官方 TLS 真池，達到 `ONLINE`、取得 accepted 工作、沒有 rejected 工作，並完成精確自檢。
RTX 4070 已使用同一 CUDA 12 二進位的最終 HiveOS 套件，在空 Pass 下取得 accepted 工作、
沒有 stale 或 rejected 工作，且通過官方 TLS 真池與 HiveOS 統計。RTX 4090 已使用公開
HiveOS／CUDA 12 套件完成預設頻率離線測試。CUDA 13 實體 GPU、其他上表型號、多 GPU、
斷線／failover 注入及長時間運行，本版未重新實測。

HiveOS 請使用 `invminer-0.1.63.tar.gz`；Miner name 填 `invminer`、Hash algorithm 填
`noid`、Template 填 `%WAL%.%WORKER_NAME%`、Pass 可留空，Extra config arguments 預設
留空。官方節點僅為 `eu.innovlab.cc:19601` 或 `hk.innovlab.cc:19601`。

NOID 開發費為有效挖礦時間的 5%。套件不會自行建立或啟用開機服務、cron、排程、登入
啟動項或容器重啟策略；本倉庫僅提供閉源二進位，使用者需自行評估相容性與信任風險。
