# INVminer v0.1.50

v0.1.50 introduces the unified multi-coin executable and the first matching
HiveOS packages. The executable is always named `invminer`; select NOID
explicitly with `--coin noid`.

> **Binary-only risk:** this release may be incompatible with a particular GPU,
> CPU, driver, OS, or future pool state. Source is not provided, so users cannot
> independently rebuild, audit, or patch it and must decide whether to trust the
> executable and published checksums. The software is provided without warranty.

## Assets

| Asset | Purpose | Archive SHA-256 | Binary SHA-256 |
|---|---|---|---|
| `invminer-v0.1.50-linux-x86_64-cuda12.tar.gz` | Broad/535-era host compatibility | `c0b1367d2f8efd63aa8e72c80fc38924fbccefb04ad910c62f5eabb5c8065f39` | `ce87b75081758cbf0fe18350b4acc732ff366952237e58462639721902a36784` |
| `invminer-v0.1.50-linux-x86_64-cuda13.tar.gz` | Driver API 13.0 / Linux driver 580+ | `c0af6addf7d1fa2dd8199208f6cbb653a20e2ef25f767d1c5e9069ac22b5d209` | `8421d5fadf141cab981129afe7964739f7f296b0c27fb885113f639177f610ef` |
| `invminer-v0.1.50-hiveos-linux-x86_64-cuda12.tar.gz` | HiveOS, CUDA 12 flavor | `5209bb4153b41ac314c3592179ffbeb755e3f557c5ac0f4a5dcf31fa594552aa` | `ce87b75081758cbf0fe18350b4acc732ff366952237e58462639721902a36784` |
| `invminer-v0.1.50-hiveos-linux-x86_64-cuda13.tar.gz` | HiveOS, CUDA 13 flavor | `fc484b5ee9a5262e2f006819c7c19acf55f4e8988c0605c88b1c4efc656313fb` | `8421d5fadf141cab981129afe7964739f7f296b0c27fb885113f639177f610ef` |

Both flavors were linked in an isolated Ubuntu 20.04 / Rust 1.96 builder. The
maximum required GNU C Library symbol is `GLIBC_2.30`; CUDA and NVML are loaded
through the host driver.

## Required command

```bash
./invminer --coin noid \
  -o stratum+ssl://stratum.innovlab.cc:19601 \
  -u YOUR_NOID_ADDRESS.RIG_NAME \
  -p x
```

For CPU-only operation, add `--cpu-only`. To select one GPU, add `--device 0`.
With no device list, all supported GPUs use one shared user-pool connection and
non-overlapping search ranges. Mining geometry, CPU thread count, batch size,
ISA flags, and `--state-dir` are not required in the normal command.

## CUDA compatibility

| GPU family | SM | CUDA 12 | CUDA 13 | v0.1.50 status |
|---|---:|---|---|---|
| Turing / CMP | `sm_75` | Supported | Unsupported | Architecture lane included; use CUDA 12 |
| RTX 30 | `sm_86` | Supported | Supported | Architecture lanes included; not physically rerun for this release |
| RTX 40 | `sm_89` | Supported | Supported | Both exact assets physically tested on RTX 4070 / driver 580.178.04 |
| RTX 50 | `sm_120` | Supported | Supported | Architecture lanes included; not physically rerun for this release |

The RTX 4070 gate used default board controls and no clock arguments. Both exact
assets loaded their native SM89 module, passed CPU/GPU exact self-tests, and
submitted accepted shares with no resolved rejected shares. The canonical
InnovLab endpoint was unavailable from the gate host, so the already-authorized
WebPKI compatibility gate was used; its endpoint and test identity are not
published. CPU-only mode also passed its exact AVX-512 self-test.

## HiveOS

The HiveOS archives contain:

```text
invminer/
  invminer
  h-manifest.conf
  h-config.sh
  h-run.sh
  h-stats.sh
  h-readme.md
```

In a Custom Miner flight sheet set Hash algorithm to `noid`, Pool URL to the TLS
endpoint above, Template to `%WAL%.%WORKER_NAME%`, and Pass to `x`. The adapter
maps them to `invminer --coin noid ...` without `eval` and reports aggregate and
per-GPU statistics through the loopback API.

## Developer fee and process behavior

NOID uses a 5% developer fee measured from effective mining time. Waiting,
connection preparation failures, and unavailable fee work are not charged. The
payout identity is not printed in normal logs or public documentation.

Extracting or running the package does not install or enable a systemd service,
cron job, scheduled task, login/startup item, registry Run key, or container
restart policy. Optional hardware controls remain disabled unless explicitly
provided.

---

# INVminer v0.1.50（繁體中文）

v0.1.50 改為統一多幣種執行檔，並首次提供對應的 HiveOS 套件。執行檔固定名為
`invminer`，NOID 必須以 `--coin noid` 明確選擇：

```bash
./invminer --coin noid \
  -o stratum+ssl://stratum.innovlab.cc:19601 \
  -u YOUR_NOID_ADDRESS.RIG_NAME \
  -p x
```

CUDA 12 套件提供最廣相容範圍，包含 `sm_75/sm_86/sm_89/sm_120` 與 fallback；
CUDA 13 套件要求 Driver API 13.0／Linux 驅動 580 或更新版本，包含
`sm_86/sm_89/sm_120`。Turing、CMP 40HX、CMP 50HX 與 RTX 2080 Ti 必須使用
CUDA 12。

兩個精確候選都已在 RTX 4070／驅動 580.178.04 上載入原生 SM89 模組、通過
CPU/GPU 逐位自檢，並取得 accepted share，已解析的 rejected 為零；測試使用預設硬體控制，
未加入時鐘參數。正式 InnovLab 入口當時無法由測試機連線，因此使用既有授權且經 WebPKI
驗證的相容門禁；實際入口與測試身份不公開。`--cpu-only` 亦通過 AVX-512 逐位自檢。

HiveOS 請使用與驅動相符的 HiveOS 壓縮包，在 Custom Miner 飛行表填入：Hash algorithm
為 `noid`、Pool URL 為上述 TLS 入口、Template 為 `%WAL%.%WORKER_NAME%`、Pass 為
`x`。未指定裝置時，多張 GPU 共用一條使用者礦池連線，搜尋空間互不重疊。

NOID 開發者費用為有效挖礦時間的 5%；等待、連線準備失敗及不可用的 Fee 工作不計費，結算身份
不會出現在一般日誌或公開文件。解壓或執行本軟體不會安裝或啟用 systemd、cron、排程任務、登入
啟動項或容器重啟策略。

本倉庫只提供閉源二進位。軟體可能與特定 GPU、CPU、驅動、作業系統或未來礦池狀態不相容；
使用者無法自行重建、稽核或修補，須自行判斷是否信任執行檔與公開摘要。本軟體不提供任何保證。
