# INVminer v0.1.54

## GPU command

```bash
./invminer --coin noid \
  -o stratum+ssl://stratum.innovlab.cc:19601 \
  -u YOUR_NOID_ADDRESS.RIG_NAME \
  -p x
```

## CPU-only command

```bash
./invminer --coin noid --cpu-only \
  -o stratum+ssl://stratum.innovlab.cc:19601 \
  -u YOUR_NOID_ADDRESS.RIG_NAME \
  -p x
```

This release supports only NOID. The executable remains `invminer`, and the
algorithm must be selected explicitly with `--coin noid`.

> **Binary-only risk:** this release may not work with every GPU, CPU, driver,
> OS, or future pool state. Source is not provided, so users cannot independently
> rebuild, audit, or patch it and must decide whether to trust the executable and
> published checksums. The software is provided without warranty.

## RTX 40 support

v0.1.54 refreshes the native `sm_89` path used by RTX 40-series GPUs. The exact
CUDA 13 release binary selected that path on an RTX 4090 / Driver 595.84, passed
the complete CPU/GPU exact self-test, and completed the short stability gate.
The canonical CUDA 12 HiveOS archive selected its reviewed `sm_89` compatibility
path on an RTX 4070 / Driver 580.178.04, connected to the configured TLS pool,
produced an accepted share with no stale or rejected share, and passed `h-stats`.

## Assets

| Asset | Purpose | Archive SHA-256 | Binary SHA-256 |
|---|---|---|---|
| `invminer-v0.1.54-linux-x86_64-cuda12.tar.gz` | Broad/535-era host compatibility | `9c0e87e3c9576d0e3914b7623083b625179e7e385cb1a9603a1376040de08468` | `25a34cdeab3a3f629b122db5c6780e181178630f2f21c4b229be1eb95f37729d` |
| `invminer-v0.1.54-linux-x86_64-cuda13.tar.gz` | Driver API 13.0 / Linux driver 580+ | `fd3893ad5adbcb7ce5687a6cd037288c72a16c126033144239b7b879ae89bda3` | `40f18e7d6765cb2a65c5cbe4dbb636ba211aabd9c330f54e3495a5322e0f9379` |
| `invminer-0.1.54.tar.gz` | Canonical HiveOS `<name>-<version>` package; broad CUDA 12 host flavor | `8f4bb8c630e98cac462ba9ea0a7390b1fc6edc0ce925b4198745bad58faff0da` | `25a34cdeab3a3f629b122db5c6780e181178630f2f21c4b229be1eb95f37729d` |

Both flavors were reproducibly linked in an isolated Ubuntu 20.04 / Rust 1.96
builder. The maximum required GNU C Library symbol is `GLIBC_2.30`; CUDA and
NVML are loaded through the host driver.

## CUDA compatibility

| GPU family | SM | CUDA 12 | CUDA 13 | v0.1.54 qualification |
|---|---:|---|---|---|
| Turing / CMP | `sm_75` | Supported | Unsupported | Existing reviewed lane retained; use CUDA 12; not physically rerun |
| RTX 30 | `sm_86` | Supported | Supported | Existing reviewed lane retained; not physically rerun |
| RTX 40 | `sm_89` | Supported | Supported | Exact CUDA 12 package passed on RTX 4070; exact CUDA 13 binary passed on RTX 4090 |
| RTX 50 | `sm_120` | Supported | Supported | Existing reviewed lane retained; not physically rerun |

## HiveOS

- Miner name: `invminer`
- Installation URL: `https://github.com/getrigeos/INVminer-Release/releases/download/v0.1.54/invminer-0.1.54.tar.gz`
- Hash algorithm: `noid`
- Wallet and worker template: `%WAL%.%WORKER_NAME%`
- Pool URL: `stratum+ssl://stratum.innovlab.cc:19601`
- Pass: `x`
- Extra config arguments: leave empty unless ordinary runtime options are needed

The HiveOS archive must remain `<miner-name>-<version>.tar.gz`; do not add the
tag `v`, platform, architecture, HiveOS, or CUDA labels. Do not place an
alternate package URL in Extra config arguments.

### Manual upgrade for older HiveOS installations

Only some older HiveOS installations need this manual replacement. Use it only when HiveOS does not update the installed custom miner after the Installation URL is changed to this release.

```bash
miner stop && cd /tmp && rm -rf invminer && rm -f invminer-0.1.54.tar.gz && wget -O invminer-0.1.54.tar.gz https://github.com/getrigeos/INVminer-Release/releases/download/v0.1.54/invminer-0.1.54.tar.gz && tar -xzf invminer-0.1.54.tar.gz && mkdir -p /hive/miners/custom/invminer && rm -f /hive/miners/custom/invminer/invminer && cp -af invminer/. /hive/miners/custom/invminer/ && chmod +x /hive/miners/custom/invminer/invminer /hive/miners/custom/invminer/h-*.sh && /hive/miners/custom/invminer/invminer --version && miner start
```

With no device list, supported GPUs share one user-pool connection and use
non-overlapping search ranges. Normal operation does not require geometry,
thread, batch, ISA, clock, or `--state-dir` arguments.

NOID uses a 5% developer fee measured from effective mining time. Waiting,
connection preparation failures, and unavailable fee work are not charged.
The payout identity is not printed in normal logs or public documentation.

Extracting or running the package does not install or enable a boot service,
cron job, scheduled task, login/startup item, or container restart policy.

---

# INVminer v0.1.54（繁體中文）

GPU 與 CPU-only 命令如頁首所示。本版只支援 NOID，執行檔固定為 `invminer`，並以
`--coin noid` 明確選擇。

v0.1.54 更新 RTX 40 系列使用的原生 `sm_89` 路徑。CUDA 13 的精確發行二進位已在
RTX 4090／Driver 595.84 選中該路徑，通過完整 CPU/GPU 精確自檢與短時穩定性門禁。
標準 CUDA 12 HiveOS 套件則在 RTX 4070／Driver 580.178.04 選中既有審核過的
`sm_89` 相容路徑，成功連線 TLS 礦池、產生 accepted share、沒有 stale 或 rejected
share，且 `h-stats` 門禁通過。其他未變更的 GPU 架構沿用既有審核資產，本版未重新實機測試。

HiveOS 請使用 `invminer-0.1.54.tar.gz`；Miner name 填 `invminer`、Hash algorithm 填
`noid`、Template 填 `%WAL%.%WORKER_NAME%`、Pass 填 `x`，Extra config arguments 預設
留空。不要改動標準套件名稱，也不要把其他下載位址填入 Extra config arguments。

### 舊版 HiveOS 手動升級

僅部分舊版 HiveOS 需要此手動覆蓋；只有在更新 Installation URL 後仍未替換已安裝的 Custom Miner 時才執行。
請使用上方已按本版本產生的完整單行命令，不要拆行或手動改寫版本。

解壓或執行套件不會自行建立或啟用任何開機服務或排程。本倉庫只提供閉源二進位，使用者需自行
評估相容性與信任風險。
