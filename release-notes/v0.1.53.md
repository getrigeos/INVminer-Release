# INVminer v0.1.53

## GPU command

```bash
./invminer --coin noid \
  -o stratum+ssl://stratum.innovlab.cc:19601 \
  -u YOUR_NOID_ADDRESS.RIG_NAME \
  -p x
```

## CPU-only command

```bash
./invminer --coin noid --cpu-only \
  -o stratum+ssl://stratum.innovlab.cc:19601 \
  -u YOUR_NOID_ADDRESS.RIG_NAME \
  -p x
```

This release supports only NOID. The executable remains `invminer`, and the
algorithm must be selected explicitly with `--coin noid`.

> **Binary-only risk:** this release may not work with every GPU, CPU, driver,
> OS, or future pool state. Source is not provided, so users cannot independently
> rebuild, audit, or patch it and must decide whether to trust the executable and
> published checksums. The software is provided without warranty.

## Reliability

Brief pool work pauses during template transitions no longer cause avoidable
user or developer-fee session reconnects. A prolonged absence of verified work
still enters bounded recovery. TLS identity verification, share validation,
reject monitoring, GPU recovery, and multi-GPU search-space separation remain
enabled.

## Assets

| Asset | Purpose | Archive SHA-256 | Binary SHA-256 |
|---|---|---|---|
| `invminer-v0.1.53-linux-x86_64-cuda12.tar.gz` | Broad/535-era host compatibility | `017db0f70e0b2dc6b5985ab9526749ae9b99513ce19d484f4857be8c37515956` | `af02ae157e6537aca8d24144e531857b2cabc27788a42445c7f0bd97416b8e6f` |
| `invminer-v0.1.53-linux-x86_64-cuda13.tar.gz` | Driver API 13.0 / Linux driver 580+ | `d63cde20d468b5389ef1cb4ed562c402e419f700597b6a9763c887a7418bb6db` | `52a72b4c07d942d92726ab328945e9d9ca9389f6f40d3685f25f2311def5135d` |
| `invminer-0.1.53.tar.gz` | Canonical HiveOS `<name>-<version>` package; broad CUDA 12 host flavor | `2b1106dacac9baf756d73ca001c9c10faa256a17559871eeb5ddc6c279ead813` | `af02ae157e6537aca8d24144e531857b2cabc27788a42445c7f0bd97416b8e6f` |

Both flavors were reproducibly linked in an isolated Ubuntu 20.04 / Rust 1.96
builder. The maximum required GNU C Library symbol is `GLIBC_2.30`; CUDA and
NVML are loaded through the host driver.

## CUDA compatibility

| GPU family | SM | CUDA 12 | CUDA 13 | v0.1.53 qualification |
|---|---:|---|---|---|
| Turing / CMP | `sm_75` | Supported | Unsupported | Architecture lane included; use CUDA 12 |
| RTX 30 | `sm_86` | Supported | Supported | Architecture lane included; not physically rerun |
| RTX 40 | `sm_89` | Supported | Supported | Both exact assets passed GPU self-tests on RTX 4070 / Driver 580.178.04 |
| RTX 50 | `sm_120` | Supported | Supported | Architecture lane included; not physically rerun |

The RTX 4070 qualification used default board controls and no clock arguments.
The canonical HiveOS package reached the configured TLS pool, produced an
accepted share with no rejected share, and passed the HiveOS statistics gate.

## HiveOS

- Miner name: `invminer`
- Installation URL: `https://github.com/getrigeos/INVminer-Release/releases/download/v0.1.53/invminer-0.1.53.tar.gz`
- Hash algorithm: `noid`
- Wallet and worker template: `%WAL%.%WORKER_NAME%`
- Pool URL: `stratum+ssl://stratum.innovlab.cc:19601`
- Pass: `x`
- Extra config arguments: leave empty unless ordinary runtime options are needed

The HiveOS archive must remain `<miner-name>-<version>.tar.gz`; do not add the
tag `v`, platform, architecture, HiveOS, or CUDA labels. Do not place an
alternate package URL in Extra config arguments.

With no device list, supported GPUs share one user-pool connection and use
non-overlapping search ranges. Normal operation does not require geometry,
thread, batch, ISA, clock, or `--state-dir` arguments.

NOID uses a 5% developer fee measured from effective mining time. Waiting,
connection preparation failures, and unavailable fee work are not charged.
The payout identity is not printed in normal logs or public documentation.

Extracting or running the package does not install or enable a boot service,
cron job, scheduled task, login/startup item, or container restart policy.

---

# INVminer v0.1.53（繁體中文）

GPU 與 CPU-only 命令如頁首所示。本版只支援 NOID，執行檔固定為 `invminer`，並以
`--coin noid` 明確選擇。

本版修正礦池切換模板時短暫暫停工作所造成的不必要重連；使用者與開發費連線都會保留，只有
長時間沒有可驗證工作時才進入有界恢復。TLS 身分驗證、share 校驗、拒絕監控、GPU 恢復與多卡
搜尋空間隔離維持啟用。

CUDA 12 包含 `sm_75/sm_86/sm_89/sm_120` 與 fallback；CUDA 13 包含
`sm_86/sm_89/sm_120`，要求 Linux Driver 580 或更新版本。兩個精確候選均已在 RTX
4070／Driver 580.178.04 使用預設硬體控制完成 GPU 自檢。標準 HiveOS 套件已連線至 TLS
礦池、產生 accepted share、沒有 rejected share，且 HiveOS 統計門禁通過。

HiveOS 請使用 `invminer-0.1.53.tar.gz`；Miner name 填 `invminer`、Hash algorithm 填
`noid`、Template 填 `%WAL%.%WORKER_NAME%`、Pass 填 `x`，Extra config arguments 預設
留空。不要改動標準套件名稱，也不要把其他下載位址填入 Extra config arguments。

解壓或執行套件不會自行建立或啟用任何開機服務或排程。本倉庫只提供閉源二進位，使用者需自行
評估相容性與信任風險。
