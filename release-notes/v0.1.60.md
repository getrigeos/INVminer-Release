# INVminer v0.1.60

## GPU command

```bash
./invminer --coin noid \
  -o stratum+ssl://eu.innovlab.cc:19601 \
  -u YOUR_NOID_ADDRESS.RIG_NAME \
  -p x
```

Use only an official NOID TLS endpoint: `eu.innovlab.cc:19601` (Europe) or
`hk.innovlab.cc:19601` (Hong Kong). This example defaults to Europe; replace
only the hostname to use Hong Kong.

## CPU-only command

```bash
./invminer --coin noid --cpu-only \
  -o stratum+ssl://eu.innovlab.cc:19601 \
  -u YOUR_NOID_ADDRESS.RIG_NAME \
  -p x
```

> **Binary-only risk:** this release may not work with every GPU, CPU, driver,
> OS, or future pool state. Source is not provided here, so users cannot
> independently rebuild, audit, or patch it and must decide whether to trust
> the executable and published checksums. The software is provided without
> warranty.

## Ada qualification update

This release updates the reviewed Ada execution profile used by RTX 40 GPUs.
The final CUDA 12 and CUDA 13 binaries passed exact CPU/GPU self-tests and
bounded runs on an RTX 4070 at default clocks. The final CUDA 13 binary also
passed the same correctness and bounded-run qualification on an RTX 4090 at
default clocks. No manual geometry or diagnostic environment setting is
required.

The CUDA 12 Ampere compatibility fallback introduced in v0.1.59 is retained.
The reviewed modules for `sm_75`, `sm_86`, and `sm_120` are retained unchanged.

## Release assets

| Asset | Purpose | Embedded lanes | Archive SHA-256 | Binary SHA-256 |
|---|---|---|---|---|
| `invminer-v0.1.60-linux-x86_64-cuda12.tar.gz` | Broad/535-era host compatibility | `sm_75`, `sm_86`, `sm_89`, `sm_120`, fallback | `4fa9beca4d20cfe80fe654c6bae8a9069e99011a5f2de45e8efe62fe7b5f2f4e` | `d63fcde51e3ebdfe88fd317894ab3e300e9d9dadb603a9be9e31d840263cd2e4` |
| `invminer-v0.1.60-linux-x86_64-cuda13.tar.gz` | Driver API 13.0 / Linux driver 580+ | `sm_86`, `sm_89`, `sm_120` | `2078801d2ce1e1faefcf7fcb6a0360a6caec257b79e522231ed973f740c653be` | `bc3488560ef90120f596fa246bc92e2f696e0447ed987a1e5439835b30e419e6` |
| `invminer-0.1.60.tar.gz` | Canonical HiveOS package; broad CUDA 12 host flavor | same CUDA 12 lanes | `6da9cf462f7fe45f284fda3ce8d16d4cd07e0a797596f2815a83dc846df551cd` | `d63fcde51e3ebdfe88fd317894ab3e300e9d9dadb603a9be9e31d840263cd2e4` |

`SHA256SUMS.txt` SHA-256:
`9cfdbe7799b29665bc213df77ad04c065205b4820276b65c198d8756a8cdb958`.

Both flavors were reproducibly linked in isolated Ubuntu 20.04 / Rust 1.96
builders. The maximum required GNU C Library symbol is `GLIBC_2.30`; CUDA and
NVML are loaded through the host driver.

## Compatibility qualification

| GPU family | SM | CUDA 12 | CUDA 13 | Tested in this release | Functional result |
|---|---:|---|---|---|---|
| RTX 2080 Ti / Turing CMP | `sm_75` | Supported | Unsupported | No | Existing reviewed CUDA 12 path retained |
| RTX 3070 Ti / RTX 30 | `sm_86` | Supported | Supported | No | Existing reviewed lanes and automatic CUDA 12 fallback retained |
| RTX 4070 / RTX 40 | `sm_89` | Supported | Supported | Yes — both final binaries and final HiveOS package | Exact self-tests and bounded runs passed; packaged TLS session reached `ONLINE` and accepted work with no stale or rejected work |
| RTX 4090 | `sm_89` | Supported | Supported | Yes — final CUDA 13 binary | Exact self-test and bounded run passed at default clocks |
| RTX 5090 / RTX 50 | `sm_120` | Supported | Supported | No | Existing reviewed Blackwell path retained |

The CUDA 12 asset requires NVIDIA Linux driver 535 or newer. The CUDA 13 asset
requires Driver API 13.0 / Linux driver 580 or newer. Disconnect/failover
injection, multi-GPU qualification, and the GPU models marked “No” above were
not rerun for this release.

## Native NVIDIA controls

The existing `--power-limit`, `--lock-core`, `--locked-core-clock`,
`--lock-mem`, and `--core-clock-offset` controls are retained. Unsupported
values, missing permissions, or unavailable optional tools produce a warning
and mining continues at the verified original/default state; an unsafe partial
rollback stops mining.

## HiveOS

- Miner name: `invminer`
- Installation URL: `https://github.com/getrigeos/INVminer-Release/releases/download/v0.1.60/invminer-0.1.60.tar.gz`
- Hash algorithm: `noid`
- Pool URL: `stratum+ssl://eu.innovlab.cc:19601`
- Wallet and worker template: `%WAL%.%WORKER_NAME%`
- Pass: `x`
- Extra config arguments: leave empty unless ordinary runtime options are needed

The HiveOS archive must remain `<miner-name>-<version>.tar.gz`; do not add the
tag `v`, platform, architecture, HiveOS, or CUDA labels.

### Manual upgrade for older HiveOS installations

Only some older HiveOS installations need this manual replacement. Use it only when HiveOS does not update the installed custom miner after the Installation URL is changed to this release.

```bash
miner stop && cd /tmp && rm -rf invminer && rm -f invminer-0.1.60.tar.gz && wget -O invminer-0.1.60.tar.gz https://github.com/getrigeos/INVminer-Release/releases/download/v0.1.60/invminer-0.1.60.tar.gz && tar -xzf invminer-0.1.60.tar.gz && mkdir -p /hive/miners/custom/invminer && rm -f /hive/miners/custom/invminer/invminer && cp -af invminer/. /hive/miners/custom/invminer/ && chmod +x /hive/miners/custom/invminer/invminer /hive/miners/custom/invminer/h-*.sh && /hive/miners/custom/invminer/invminer --version && miner start
```

### 舊版 HiveOS 手動升級

僅部分舊版 HiveOS 需要此手動覆蓋；只有在更新 Installation URL 後仍未替換已安裝的 Custom Miner 時才執行。

## Developer fee and startup boundary

NOID uses a 5% developer fee measured from effective mining time. Waiting,
connection preparation failures, and unavailable fee work are not charged.
Extracting or running the package does not install or enable a boot service,
cron job, scheduled task, login/startup item, or container restart policy.

---

# INVminer v0.1.60（繁體中文）

本版更新 RTX 40 系列使用的既有審核執行設定。RTX 4070 已使用最終 CUDA 12、CUDA 13
二進位及最終 HiveOS 套件完成精確 CPU/GPU 自檢、受控運行、官方 TLS 真池與 HiveOS
統計驗收；RTX 4090 亦以最終 CUDA 13 二進位在預設時脈完成自檢及受控運行。

CUDA 12 的 Ampere 自動相容 fallback 繼續保留。RTX 30、Turing/CMP、RTX 50、本版未列出的
驅動組合、多 GPU 及斷線／failover 注入沒有重新實測，請依上表選擇套件並評估相容性。

HiveOS 請使用 `invminer-0.1.60.tar.gz`；Miner name 填 `invminer`、Hash algorithm
填 `noid`、Template 填 `%WAL%.%WORKER_NAME%`、Pass 填 `x`，Extra config arguments
預設留空。官方節點僅為 `eu.innovlab.cc:19601` 或 `hk.innovlab.cc:19601`。

NOID 開發費為有效挖礦時間的 5%。套件不會自行建立或啟用開機服務、cron、排程、登入
啟動項或容器重啟策略；本倉庫僅提供閉源二進位，使用者需自行評估相容性與信任風險。
